<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 매칭 : 약도와 현실의 길을 일치 시키는 과정.
	퀴리문 끝에 ; 붙이지 않음.
	resultType은 대문자로 시작하지 않음. String 이전에 문자열로 닼앗 처리하는 과정과는 비슷하면서 다름. -->
	
<mapper namespace="com.commu.mapper.TimeMapper">

<select id="getTime" resultType="string">
	SELECT now()
</select>

<select id="getMember" resultType="string" >
SELECT * FROM big_member
</select>

</mapper>